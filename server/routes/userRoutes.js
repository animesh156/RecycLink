const express = require('express')
const router = express.Router()

router.post('/register', () => {
    // register route controller will be here
})

router.post('/sign', () => {
    // login route logic controller will be here
})

module.exports = router;